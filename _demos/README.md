## need to implement 

* json preset to create preset and fetch presets 
* Use preset to generate preview video - locally and on server with github action
* Use preset to Automate with script to generate 100 of videos
    - Input json with all the values for fields 
    - Generate video for each json file 
    - Save generated video in output directory 
    - Save generated video in output directory with thumbnail
    - 


- Create a Svelte APP to create a unify SIngle SIte Application Experience instead of many individual html sites 


- Create UI for generating preview videos with preset 
- Create UI for automating with script to generate 100 of videos 
    - Input json with all the values for fields 
    - Generate video for each json file 
    - Save generated video in output directory 
    - Save generated video in output directory with thumbnail
    - 
    