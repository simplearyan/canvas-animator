{
  "name": "canvas-animator-studio",
  "version": "1.0.0",
  "description": "Blueprint-themed studio for high-fidelity animation tools and templates.",
  "main": "generate-index.js",
  "scripts": {
    "dev": "vite",
    "build-vite": "vite build",
    "index": "node generate-index.js",
    "preview": "node generate-preview.js",
    "preview:force": "node generate-preview.js --force",
    "build": "node generate-preview.js && node generate-index.js && vite build"
  },
  "dependencies": {
    "mediabunny": "^1.40.1",
    "puppeteer": "^24.2.1"
  },
  "devDependencies": {
    "glob": "^13.0.6",
    "vite": "^8.0.9"
  }
}
